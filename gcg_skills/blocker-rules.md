---
id: blocker-rules
triggers:
  keywords:
    - Blocker
    - 阻擋
    - battle
---

# Blocker 規則技能

- 只有 active 且具有 `Blocker` 的 Unit 能宣告阻擋。
- rested Unit 即使有 `Blocker` 也不能阻擋。
- 普通 Unit 不會因為 HP 高就保護基地或盾牌。
- 若要防守，優先找顯示為合法的 `block <slot>` / `阻擋 <欄位>` 指令。
